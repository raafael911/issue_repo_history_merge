<script type="text/javascript">
  <!--
  function handler_toggle_revisions () {
    var checkbox = $("#show_revisions");
    if (checkbox.attr('checked')) {
      $(".changeset").css("display", "block");
    }
    else {
      $(".changeset").css("display", "none");
    }
  }
  //-->
</script>
<style type="text/css">
  <% if session[:show_revisions] != "on" %>
  	div.changeset { display:none; }
  <% end %>
</style>

<%= form_tag '/issues/toggle_revisions', remote: true , id: 'toggle_revision_visibility_form' do %>
	<%= check_box_tag 'show_revisions', session[:show_revisions], session[:show_revisions], onclick: 'handler_toggle_revisions(); $("#toggle_revision_visibility_form").submit();' %>
<% end %>
	
<%= label_tag 'show_revisions', l(:show_changesets) %>
<br/>